{
  "name": "finaizen-backend",
  "version": "1.0.0",
  "description": "Backend API REST para Finaizen - Sistema de gestión financiera personal",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "json-server": "json-server --watch db.json --port 3000 --host localhost",
    "start": "json-server --watch db.json --port 3000",
    "dev": "json-server --watch db.json --port 3000",
    "express": "node server.js",
    "express:dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "express",
    "api",
    "rest",
    "finaizen",
    "backend"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "json-server": "^1.0.0-beta.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
